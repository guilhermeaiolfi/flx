<a {{action "read" target="view"}}>Post</a> Index, {{name}} : {{mother.name}}"

<br />
<br />
<a href="{{url "post.edit" id="1"}}">Go to edit</a>
<br />
<ul>
	{{#each item in items}}
		<li>{{item.name}}</li>
	{{/each}}
</ul>